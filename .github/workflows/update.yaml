name: update-flake
on:
  schedule:
    - cron: '0 0 * * 6'
  workflow_dispatch:

jobs:
  update:
    runs-on: ubuntu-latest
    outputs:
      changed: ${{ steps.check.outputs.changed }}
    steps:
      - uses: actions/checkout@v5

      - uses: DeterminateSystems/nix-installer-action@main

      - name: Update flake inputs
        run: nix flake update

      - name: Check for changes
        id: check
        run: |
          if git diff --quiet flake.lock; then
            echo "No changes to flake.lock"
            echo "changed=false" >> "$GITHUB_OUTPUT"
          else
            echo "flake.lock updated"
            echo "changed=true" >> "$GITHUB_OUTPUT"
          fi

      - name: Upload flake.lock
        if: steps.check.outputs.changed == 'true'
        uses: actions/upload-artifact@v7
        with:
          name: update-output
          path: flake.lock
          retention-days: 1

  build:
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            system: x86_64-linux
            host-attr: nixosConfigurations
          - os: macos-latest
            system: aarch64-darwin
            host-attr: darwinConfigurations
    runs-on: ${{ matrix.os }}
    needs: update
    if: needs.update.outputs.changed == 'true'
    steps:
      - uses: actions/checkout@v5

      - uses: actions/download-artifact@v8
        with:
          name: update-output

      - uses: DeterminateSystems/nix-installer-action@main

      - name: Build ${{ matrix.host-attr }}
        run: |
          for host in $(nix eval ".#${{ matrix.host-attr }}" --apply 'x: builtins.attrNames x' --json | jq -r '.[]'); do
            nix build ".#${{ matrix.host-attr }}.${host}.config.system.build.toplevel" --no-link
          done

      - name: Build matching home configs
        run: |
          for config in $(nix eval .#homeConfigurations --apply 'x: builtins.attrNames x' --json | jq -r '.[]'); do
            sys=$(nix eval ".#homeConfigurations.\"${config}\".activationPackage.system" --raw 2>/dev/null || true)
            if [ "$sys" = "${{ matrix.system }}" ]; then
              nix build ".#homeConfigurations.\"${config}\".activationPackage" --no-link
            fi
          done

  pr:
    runs-on: ubuntu-latest
    needs: [update, build]
    if: needs.update.outputs.changed == 'true'
    permissions:
      contents: write
      pull-requests: write
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - name: Download flake.lock
        uses: actions/download-artifact@v8
        with:
          name: update-output

      - name: Commit and push
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "git.bot@yanlincs.com"
          git checkout -B flake-update
          git add flake.lock
          git commit -m "auto: update flake inputs"
          git push -f origin flake-update

      - name: Create or update pull request
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          run_url="${GITHUB_SERVER_URL}/${GITHUB_REPOSITORY}/actions/runs/${GITHUB_RUN_ID}"
          body="Build results: see the [workflow run](${run_url})."
          existing=$(gh pr list --head flake-update --state open --json number --jq '.[0].number')
          if [ -n "$existing" ]; then
            echo "PR #${existing} already exists, updating body"
            gh pr edit "$existing" --body "$body"
          else
            gh pr create --title "Update flake inputs" --body "$body" --head flake-update --base master
          fi
