# Home-manager configuration for himawari, a macOS host.
# It carries no settings of its own and relies entirely on the macOS platform default.

{ config, pkgs, ... }:

{
  imports = [
    ../home-default.nix
  ];

}
